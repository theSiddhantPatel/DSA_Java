// package Java DSA.LC.contest.weekly449;

public class gridPartition {
    public static boolean canPartitionGrid(int[][] grid) {
        if (grid.length == grid[0].length) {
            
            for () {

            }

        }
        return  true;
    }
}
